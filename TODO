migrations:
	alte wp8 sheets uebernehmen

controller:
	llna f compounds
	sort treatments
	home/index updaten
	affy file download, sonst ohne link

	compounds edit (training compounds)
	experiments edit (evtl temporaer disabeln)
	excel upload f neues template aktualisieren

deploy:
	costanza daten einfuegen: per hand (=test)
	francois daten einfuegen

exception handling

done
	protokolle richtig zuordnen (samples, treatments, measurements, calculations)
	cleanup (nicht benuetzte tabellen entfernen, daten checken u ggf entfernen)
